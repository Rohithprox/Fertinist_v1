
  # Convert wireframe to code

  This is a code bundle for Convert wireframe to code. The original project is available at https://www.figma.com/design/JVSVelmVlnCzmIDZ98GF3r/Convert-wireframe-to-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  